

// Boolean : Mantıksal değerleri ifade eder.
// İki farklı değer almaktadır. true(doğru) ve false(yanlış)
// Boolean ifadeleri if yapılarında kullanılmaktadır.

import UIKit

var adımHalilMidir : Bool = true

if adımHalilMidir {
    print ("Adım dogrudur") // adım doğruysa bu blok çalışır.
}else{
    print ("Adım yanlıstır") // adım yanlışsa bu blok çalışır.
}
